export const BRAND_MUAG_AV_ENDPOINTS = {
    BRAND_MUAG_AV:(page:number,limit:number)=> `/land/selectAll?page=${page}&limit=${limit}`,
    // BRAND_MUAG_AV:(page:number,limit:number)=> `/land/selectAll?page=${page}&limit=${limit}`,
    // BRAND_MUAG_AV: '/land/selectAll?page=0&limit=12',
    
};
